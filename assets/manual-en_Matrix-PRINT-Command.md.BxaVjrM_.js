import{_ as t,o,c as a,a2 as d}from"./chunks/framework.Bg-Zxr5O.js";const p=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/Matrix-PRINT-Command.md","filePath":"manual-en/Matrix-PRINT-Command.md","lastUpdated":1785684581000}'),s={name:"manual-en/Matrix-PRINT-Command.md"};function n(i,e,r,c,m,l){return o(),a("div",null,[...e[0]||(e[0]=[d(`<h4 id="_16-4-5-the-print-command" tabindex="-1">16.4.5 The <code>PRINT</code> Command <a class="header-anchor" href="#_16-4-5-the-print-command" aria-label="Permalink to &quot;16.4.5 The \`PRINT\` Command&quot;">​</a></h4><pre><code>PRINT [_expression_]
      [/FORMAT=_format_]
      [/TITLE=_title_]
      [/SPACE={NEWPAGE _|_ _n_}]
      [{/RLABELS=_string_ … _|_ /RNAMES=_expression_}]
      [{/CLABELS=_string_ … _|_ /CNAMES=_expression_}].
</code></pre><p>The <code>PRINT</code> command is commonly used to display a matrix. It evaluates the restricted expression, if present, and outputs it either as text or a pivot table, depending on the setting of <code>MDISPLAY</code> (see <a href="./SET#SET-MDISPLAY">SET MDISPLAY</a>).</p><p>Use the <code>FORMAT</code> subcommand to specify a format, such as <code>F8.2</code>, for displaying the matrix elements. <code>FORMAT</code> is optional for numerical matrices. When it is omitted, PSPP chooses how to format entries automatically using m, the magnitude of the largest-magnitude element in the matrix to be displayed:</p><ol><li>If <em>m &lt; 10^{11}</em> and the matrix’s elements are all integers, PSPP chooses the narrowest <code>F</code> format that fits m plus a sign. For example, if the matrix is <code>{1:10}</code>, then <em>m = 10</em> , which fits in 3 columns with room for a sign, the format is <code>F3.0</code>.</li><li>Otherwise, if <em>m ≥ 10^9</em> or <em>m ≤ 10^{-4}</em>, PSPP scales all of the numbers in the matrix by <em>10^x</em> , where x is the exponent that would be used to display m in scientific notation. For example, for <em>m = 5.123×10^{20}</em>, the scale factor is <em>10^{20}</em>. PSPP displays the scaled values in format <code>F13.10</code> and notes the scale factor in the output.</li><li>Otherwise, PSPP displays the matrix values, without scaling, in format <code>F13.10</code>.</li></ol><p>The optional <code>TITLE</code> subcommand specifies a title for the output text or table, as a quoted string. When it is omitted, the syntax of the matrix expression is used as the title.</p><p>Use the <code>SPACE</code> subcommand to request extra space above the matrix output. With a numerical argument, it adds the specified number of lines of blank space above the matrix. With <code>NEWPAGE</code> as an argument, it prints the matrix at the top of a new page. The <code>SPACE</code> subcommand has no effect when a matrix is output as a pivot table.</p><p>The <code>RLABELS</code> and <code>RNAMES</code> subcommands, which are mutually exclusive, can supply a label to accompany each row in the output. With <code>RLABELS</code>, specify the labels as comma-separated strings or other tokens. With <code>RNAMES</code>, specify a single expression that evaluates to a vector of strings. Either way, if there are more labels than rows, the extra labels are ignored, and if there are more rows than labels, the extra rows are unlabeled. For output to a pivot table with <code>RLABELS</code>, the labels can be any length; otherwise, the labels are truncated to 8 bytes.</p><p>The <code>CLABELS</code> and <code>CNAMES</code> subcommands work for labeling columns as <code>RLABELS</code> and <code>RNAMES</code> do for labeling rows.</p><p>When the expression is omitted, <code>PRINT</code> does not output a matrix. Instead, it outputs only the text specified on <code>TITLE</code>, if any, preceded by any space specified on the <code>SPACE</code> subcommand, if any. Any other subcommands are ignored, and the command acts as if <code>MDISPLAY</code> is set to <code>TEXT</code> regardless of its actual setting.</p><p>The following syntax demonstrates two different ways to label the rows and columns of a matrix with <code>PRINT</code>:</p><pre><code>MATRIX.
COMPUTE m={1, 2, 3; 4, 5, 6; 7, 8, 9}.
PRINT m/RLABELS=a, b, c/CLABELS=x, y, z.

COMPUTE rlabels={&quot;a&quot;, &quot;b&quot;, &quot;c&quot;}.
COMPUTE clabels={&quot;x&quot;, &quot;y&quot;, &quot;z&quot;}.
PRINT m/RNAMES=rlabels/CNAMES=clabels.
END MATRIX.
</code></pre><p>With <code>MDISPLAY=TEXT</code> (the default), this program outputs the following (twice):</p><pre><code>m
                x        y        z
a               1        2        3
b               4        5        6
c               7        8        9
</code></pre><p>With ‘SET MDISPLAY=TABLES.’ added above ‘MATRIX.’, the output becomes the following (twice):</p><table tabindex="0"><thead><tr><th></th><th>x</th><th>y</th><th>z</th></tr></thead><tbody><tr><td>a</td><td>1</td><td>2</td><td>3</td></tr><tr><td>b</td><td>4</td><td>5</td><td>6</td></tr><tr><td>c</td><td>7</td><td>8</td><td>9</td></tr></tbody></table>`,16)])])}const u=t(s,[["render",n]]);export{p as __pageData,u as default};
