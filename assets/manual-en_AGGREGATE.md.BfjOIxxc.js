import{_ as a,o as t,c as o,a2 as i}from"./chunks/framework.Bg-Zxr5O.js";const g=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/AGGREGATE.md","filePath":"manual-en/AGGREGATE.md","lastUpdated":1785684581000}'),s={name:"manual-en/AGGREGATE.md"};function d(r,e,n,c,l,h){return t(),o("div",null,[...e[0]||(e[0]=[i(`<h3 id="_12-1-aggregate" tabindex="-1">12.1 AGGREGATE <a class="header-anchor" href="#_12-1-aggregate" aria-label="Permalink to &quot;12.1 AGGREGATE&quot;">​</a></h3><pre><code>AGGREGATE
        [OUTFILE={*,’file_name’,file_handle} [MODE={REPLACE,ADDVARIABLES}]]
        [/MISSING=COLUMNWISE]
        [/PRESORTED]
        [/DOCUMENT]
        [/BREAK=var_list]
        /dest_var[’label’]…=agr_func(src_vars[, args]…)…
</code></pre><p><code>AGGREGATE</code> summarizes groups of cases into single cases. It divides cases into groups that have the same values for one or more variables called <em>break variables</em>. Several functions are available for summarizing case contents.</p><p>The <code>AGGREGATE</code> syntax consists of subcommands to control its behavior, all of which are optional, followed by one or more destination variable assigments, each of which uses an aggregation function to define how it is calculated.</p><p>The <code>OUTFILE</code> subcommand, which must be first, names the destination for <code>AGGREGATE</code> output. It may name a system file by file name or file handle (see <a href="./File-Handles">File Handles</a>), a dataset by its name (see <a href="./Datasets">Datasets</a>), or ‘*’ to replace the active dataset. <code>AGGREGATE</code> writes its output to this file.</p><p>With <code>OUTFILE=*</code> only, <code>MODE</code> may be specified immediately afterward with the value <code>ADDVARIABLES</code> or <code>REPLACE</code>:</p><ul><li>With <code>REPLACE</code>, the default, the active dataset is replaced by a new dataset which contains just the break variables and the destination varibles. The new file contains as many cases as there are unique combinations of the break variables.</li><li>With <code>ADDVARIABLES</code>, the destination variables are added to those in the existing active dataset. Cases that have the same combination of values in their break variables receive identical values for the destination variables. The number of cases in the active dataset remains unchanged. The data must be sorted on the break variables, that is, <code>ADDVARIABLES</code> implies <code>PRESORTED</code></li></ul><p>If <code>OUTFILE</code> is omitted, <code>AGGREGATE</code> acts as if <code>OUTFILE=* MODE=ADDVARIABLES</code> were specified.</p><p>By default, <code>AGGREGATE</code> first sorts the data on the break variables. If the active dataset is already sorted or grouped by the break variables, specify <code>PRESORTED</code> to save time. With <code>MODE=ADDVARIABLES</code>, the data must be pre-sorted.</p><p>Specify <code>DOCUMENT</code> to copy the documents from the active dataset into the aggregate file (see <a href="./DOCUMENT">DOCUMENT</a>). Otherwise, the aggregate file does not contain any documents, even if the aggregate file replaces the active dataset.</p><p>Normally, <code>AGGREGATE</code> produces a non-missing value whenever there is enough non-missing data for the aggregation function in use, that is, just one non-missing value or, for the <code>SD</code> and <code>SD.</code> aggregation functions, two non-missing values. Specify <code>/MISSING=COLUMNWISE</code> to make <code>AGGREGATE</code> output a missing value when one or more of the input values are missing.</p><p>The <code>BREAK</code> subcommand is optionally but usually present. On <code>BREAK</code>, list the variables used to divide the active dataset into groups to be summarized.</p><p><code>AGGREGATE</code> is particular about the order of subcommands. <code>OUTFILE</code> must be first, followed by <code>MISSING</code>. <code>PRESORTED</code> and <code>DOCUMENT</code> follow <code>MISSING</code>, in either order, followed by <code>BREAK</code>, then followed by aggregation variable specifications.</p><p>At least one set of aggregation variables is required. Each set comprises a list of aggregation variables, an equals sign (‘=’), the name of an aggregation function (see the list below), and a list of source variables in parentheses. A few aggregation functions do not accept source variables, and some aggregation functions expect additional arguments after the source variable names.</p><p><code>AGGREGATE</code> typically creates aggregation variables with no variable label, value labels, or missing values. Their default print and write formats depend on the aggregation function used, with details given in the table below. A variable label for an aggregation variable may be specified just after the variable’s name in the aggregation variable list.</p><p>Each set must have exactly as many source variables as aggregation variables. Each aggregation variable receives the results of applying the specified aggregation function to the corresponding source variable.</p><p>The following aggregation functions may be applied only to numeric variables:</p><p><code>MEAN(var_name…)</code></p><p>Arithmetic mean. Limited to numeric values. The default format is F8.2.</p><p><code>MEDIAN(var_name…)</code></p><p>The median value. Limited to numeric values. The default format is F8.2.</p><p><code>SD(var_name…)</code></p><p>Standard deviation of the mean. Limited to numeric values. The default format is F8.2.</p><p><code>SUM(var_name…)</code></p><p>Sum. Limited to numeric values. The default format is F8.2.</p><p>These aggregation functions may be applied to numeric and string variables:</p><p><code>CGT(var_name…, value)</code><code>CLT(var_name…, value)</code><code>CIN(var_name…, low, high)</code><code>COUT(var_name…, low, high)</code></p><p>Total weight of cases greater than or less than value or inside or outside the closed range [low,high], respectively. The default format is F5.3.</p><p><code>FGT(var_name…, value)</code><code>FLT(var_name…, value)</code><code>FIN(var_name…, low, high)</code><code>FOUT(var_name…, low, high)</code></p><p>Fraction of values greater than or less than value or inside or outside the closed range [low,high], respectively. The default format is F5.3.</p><p><code>FIRST(var_name…)</code><code>LAST(var_name…)</code></p><p>First or last non-missing value, respectively, in break group. The aggregation variable receives the complete dictionary information from the source variable. The sort performed by <code>AGGREGATE</code> (and by <code>SORT CASES</code>) is stable. This means that the first (or last) case with particular values for the break variables before sorting is also the first (or last) case in that break group after sorting.</p><p><code>MIN(var_name…)</code><code>MAX(var_name…)</code></p><p>Minimum or maximum value, respectively. The aggregation variable receives the complete dictionary information from the source variable.</p><p><code>N(var_name…)</code><code>NMISS(var_name…)</code></p><p>Total weight of non-missing or missing values, respectively. The default format is F7.0 if weighting is not enabled, F8.2 if it is (see <a href="./WEIGHT">WEIGHT</a>).</p><p><code>NU(var_name…)</code><code>NUMISS(var_name…)</code></p><p>Count of non-missing or missing values, respectively, ignoring case weights. The default format is F7.0.</p><p><code>PGT(var_name…, value)</code><code>PLT(var_name…, value)</code><code>PIN(var_name…, low, high)</code><code>POUT(var_name…, low, high)</code></p><p>Percentage between 0 and 100 of values greater than or less than VALUE or inside or outside the closed range [low,high], respectively. The default format is F5.1.</p><p>These aggregation functions do not accept source variables:</p><p><code>N</code></p><p>Total weight of cases aggregated to form this group. The default format is F7.0 if weighting is not enabled, F8.2 if it is (see <a href="./WEIGHT">WEIGHT</a>).</p><p><code>NU</code></p><p>Count of cases aggregated to form this group, ignoring case weights. The default format is F7.0.</p><p>Aggregation functions compare string values in terms of internal character codes. On most modern computers, this is ASCII or a superset thereof.</p><p>The aggregation functions listed above exclude all user-missing values from calculations. To include user-missing values, insert a period (‘.’) at the end of the function name. (<em>e.g.</em> ‘SUM.’). (Be aware that specifying such a function as the last token on a line causes the period to be interpreted as the end of the command.)</p><p><code>AGGREGATE</code> both ignores and cancels the current <code>SPLIT FILE</code> settings (see <a href="./SPLIT-FILE">SPLIT FILE</a>).</p><h4 id="_12-1-1-aggregate-example" tabindex="-1">12.1.1 Aggregate Example <a class="header-anchor" href="#_12-1-1-aggregate-example" aria-label="Permalink to &quot;12.1.1 Aggregate Example&quot;">​</a></h4><p>The personnel.sav dataset provides the occupations and salaries of many individuals. For many purposes however such detailed information is not interesting, but often the aggregated statistics of each occupation are of interest. In Example 12.1 the <code>AGGREGATE</code> command is used to calculate the mean, the median and the standard deviation of each occupation.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET FILE=&quot;personnel.sav&quot;. AGGREGATE OUTFILE=* MODE=REPLACE         /BREAK=occupation         /occ_mean_salary=MEAN(salary)         /occ_median_salary=MEDIAN(salary)         /occ_std_dev_salary=SD(salary). LIST.</span></span></code></pre></div><p><strong>Example 12.1:</strong> Calculating aggregated statistics from the personnel.sav file.</p><p>Since we chose the ‘MODE=REPLACE’ option, in Results 12.1 cases for the individual persons are no longer present. They have each been replaced by a single case per aggregated value.</p><table tabindex="0"><thead><tr><th>occupation</th><th>occ_mean_salary</th><th>occ_median_salary</th><th>occ_std_dev_salary</th></tr></thead><tbody><tr><td>Artist</td><td>37836.18</td><td>34712.50</td><td>7631.48</td></tr><tr><td>Baker</td><td>45075.20</td><td>45075.20</td><td>4411.21</td></tr><tr><td>Barrister</td><td>39504.00</td><td>39504.00</td><td>.</td></tr><tr><td>Carpenter</td><td>39349.11</td><td>36190.04</td><td>7453.40</td></tr><tr><td>Cleaner</td><td>41142.50</td><td>39647.49</td><td>14378.98</td></tr><tr><td>Cook</td><td>40357.79</td><td>43194.00</td><td>11064.51</td></tr><tr><td>Manager</td><td>46452.14</td><td>45657.56</td><td>6901.69</td></tr><tr><td>Mathematician</td><td>34531.06</td><td>34763.06</td><td>5267.68</td></tr><tr><td>Painter</td><td>45063.55</td><td>45063.55</td><td>15159.67</td></tr><tr><td>Payload Specialist</td><td>34355.72</td><td>34355.72</td><td>.</td></tr><tr><td>Plumber</td><td>40413.91</td><td>40410.00</td><td>4726.05</td></tr><tr><td>Scientist</td><td>36687.07</td><td>36803.83</td><td>10873.54</td></tr><tr><td>Scrientist</td><td>42530.65</td><td>42530.65</td><td>.</td></tr><tr><td>Tailor</td><td>34586.79</td><td>34586.79</td><td>3728.98</td></tr></tbody></table><p><strong>Results 12.1:</strong> Aggregated mean, median and standard deviation per <strong>occupation</strong>.</p><p>Note that some values for the standard deviation are blank. This is because there is only one case with the respective occupation.</p>`,56)])])}const u=a(s,[["render",d]]);export{g as __pageData,u as default};
