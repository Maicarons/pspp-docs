import{_ as t,o as a,c as s,a2 as i}from"./chunks/framework.Bg-Zxr5O.js";const m=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/COMPUTE.md","filePath":"manual-en/COMPUTE.md","lastUpdated":1785684581000}'),o={name:"manual-en/COMPUTE.md"};function d(n,e,r,h,l,c){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h3 id="_12-3-compute" tabindex="-1">12.3 COMPUTE <a class="header-anchor" href="#_12-3-compute" aria-label="Permalink to &quot;12.3 COMPUTE&quot;">​</a></h3><pre><code>COMPUTE variable = expression.
</code></pre><p>or</p><pre><code>COMPUTE vector(index) = expression.
</code></pre><p><code>COMPUTE</code> assigns the value of an expression to a target variable. For each case, the expression is evaluated and its value assigned to the target variable. Numeric and string variables may be assigned. When a string expression’s width differs from the target variable’s width, the string result of the expression is truncated or padded with spaces on the right as necessary. The expression and variable types must match.</p><p>For numeric variables only, the target variable need not already exist. Numeric variables created by <code>COMPUTE</code> are assigned an <code>F8.2</code> output format. String variables must be declared before they can be used as targets for <code>COMPUTE</code>.</p><p>The target variable may be specified as an element of a vector (see <a href="./VECTOR">VECTOR</a>). In this case, an expression index must be specified in parentheses following the vector name. The expression index must evaluate to a numeric value that, after rounding down to the nearest integer, is a valid index for the named vector.</p><p>Using <code>COMPUTE</code> to assign to a variable specified on <code>LEAVE</code> (see <a href="./LEAVE">LEAVE</a>) resets the variable’s left state. Therefore, <code>LEAVE</code> should be specified following <code>COMPUTE</code>, not before.</p><p><code>COMPUTE</code> is a transformation. It does not cause the active dataset to be read.</p><p>When <code>COMPUTE</code> is specified following <code>TEMPORARY</code> (see <a href="./TEMPORARY">TEMPORARY</a>), the <code>LAG</code> function may not be used (see <a href="./Miscellaneous-Functions#LAG">LAG</a>).</p><h4 id="_12-3-1-compute-examples" tabindex="-1">12.3.1 Compute Examples <a class="header-anchor" href="#_12-3-1-compute-examples" aria-label="Permalink to &quot;12.3.1 Compute Examples&quot;">​</a></h4><p>The dataset physiology.sav contains the height and weight of persons. For some purposes, neither height nor weight alone is of interest. Epidemiologists are often more interested in the <em>body mass index</em> which can sometimes be used as a predictor for clinical conditions. The body mass index is defined as the weight of the person in kilograms divided by the square of the person’s height in metres. 4</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>get file=&#39;physiology.sav&#39;.  * height is in mm so we must divide by 1000 to get metres. compute bmi = weight / (height/1000)**2. variable label bmi &quot;Body Mass Index&quot;.  descriptives /weight height bmi.</span></span></code></pre></div><p><strong>Example 12.3:</strong> Computing the body mass index from <strong>weight</strong> and <strong>height</strong></p><p>Example 12.3 shows how you can use <code>COMPUTE</code> to generate a new variable called <strong>bmi</strong> and have every case’s value calculated from the existing values of <strong>weight</strong> and <strong>height</strong>. It also shows how you can add a label to this new variable (see <a href="./VARIABLE-LABELS">VARIABLE LABELS</a>), so that a more descriptive label appears in subsequent analyses, and this can be seen in the ouput from the <code>DESCRIPTIVES</code> command in Results 12.2.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span></span></span></code></pre></div><p><strong>Screenshot 12.2:</strong> Using the dialog box to generate a new variable and compute its values</p><p>The expression which follows the ‘=’ sign can be as complicated as necessary. See <a href="./Expressions">Expressions</a> for a precise description of the language accepted. Normally it is easiest to enter the code directly, however there is a dialog box available if desired. This is illustrated in Screenshot 12.2. One advantage is that it offers a list of mathematical functions which can be selected and pasted into the expression.</p><table tabindex="0"><thead><tr><th></th><th>N</th><th>Mean</th><th>Std Dev</th><th>Minimum</th><th>Maximum</th></tr></thead><tbody><tr><td>Weight in kilograms</td><td>40</td><td>72.12</td><td>26.70</td><td>-55.6</td><td>92.1</td></tr><tr><td>Height in millimeters</td><td>40</td><td>1677.12</td><td>262.87</td><td>179</td><td>1903</td></tr><tr><td>Body Mass Index</td><td>40</td><td>67.46</td><td>274.08</td><td>-21.62</td><td>1756.82</td></tr><tr><td>Valid N (listwise)</td><td>40</td><td></td><td></td><td></td><td></td></tr><tr><td>Missing N (listwise)</td><td>0</td><td></td><td></td><td></td><td></td></tr></tbody></table><p><strong>Results 12.2:</strong> An analysis which includes <strong>bmi</strong> in its results</p><h4 id="footnotes" tabindex="-1">Footnotes <a class="header-anchor" href="#footnotes" aria-label="Permalink to &quot;Footnotes&quot;">​</a></h4><h5 id="_4" tabindex="-1">(4) <a class="header-anchor" href="#_4" aria-label="Permalink to &quot;(4)&quot;">​</a></h5><p>Since BMI is a quantity with a ratio scale and has units, the term “index” is a misnomer, but that is what it is called.</p>`,23)])])}const g=t(o,[["render",d]]);export{m as __pageData,g as default};
