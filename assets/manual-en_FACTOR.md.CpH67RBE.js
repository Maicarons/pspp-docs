import{_ as o,o as a,c as t,a2 as i}from"./chunks/framework.Bg-Zxr5O.js";const m=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/FACTOR.md","filePath":"manual-en/FACTOR.md","lastUpdated":1785684581000}'),n={name:"manual-en/FACTOR.md"};function d(s,e,c,r,l,h){return a(),t("div",null,[...e[0]||(e[0]=[i(`<h3 id="_15-8-factor" tabindex="-1">15.8 FACTOR <a class="header-anchor" href="#_15-8-factor" aria-label="Permalink to &quot;15.8 FACTOR&quot;">​</a></h3><pre><code>FACTOR  {
         VARIABLES=var_list,
         MATRIX IN ({CORR,COV}={*,file_spec})
        }

        [ /METHOD = {CORRELATION, COVARIANCE} ]

        [ /ANALYSIS=var_list ]

        [ /EXTRACTION={PC, PAF}]

        [ /ROTATION={VARIMAX, EQUAMAX, QUARTIMAX, PROMAX[(k)], NOROTATE}]

        [ /PRINT=[INITIAL] [EXTRACTION] [ROTATION] [UNIVARIATE] [CORRELATION] [COVARIANCE] [DET] [KMO] [AIC] [SIG] [ALL] [DEFAULT] ]

        [ /PLOT=[EIGEN] ]

        [ /FORMAT=[SORT] [BLANK(n)] [DEFAULT] ]

        [ /CRITERIA=[FACTORS(n)] [MINEIGEN(l)] [ITERATE(m)] [ECONVERGE (delta)] [DEFAULT] ]

        [ /MISSING=[{LISTWISE, PAIRWISE}] [{INCLUDE, EXCLUDE}] ]
</code></pre><p>The <code>FACTOR</code> command performs Factor Analysis or Principal Axis Factoring on a dataset. It may be used to find common factors in the data or for data reduction purposes.</p><p>The <code>VARIABLES</code> subcommand is required (unless the <code>MATRIX IN</code> subcommand is used). It lists the variables which are to partake in the analysis. (The <code>ANALYSIS</code> subcommand may optionally further limit the variables that participate; it is useful primarily in conjunction with <code>MATRIX IN</code>.)</p><p>If <code>MATRIX IN</code> instead of <code>VARIABLES</code> is specified, then the analysis is performed on a pre-prepared correlation or covariance matrix file instead of on individual data cases. Typically the matrix file will have been generated by <code>MATRIX DATA</code> (see <a href="./MATRIX-DATA">MATRIX DATA</a>) or provided by a third party. If specified, <code>MATRIX IN</code> must be followed by ‘COV’ or ‘CORR’, then by ‘=’ and file_spec all in parentheses. file_spec may either be an asterisk, which indicates the currently loaded dataset, or it may be a file name to be loaded. See <a href="./MATRIX-DATA">MATRIX DATA</a>, for the expected format of the file.</p><p>The <code>/EXTRACTION</code> subcommand is used to specify the way in which factors (components) are extracted from the data. If <code>PC</code> is specified, then Principal Components Analysis is used. If <code>PAF</code> is specified, then Principal Axis Factoring is used. By default Principal Components Analysis is used.</p><p>The <code>/ROTATION</code> subcommand is used to specify the method by which the extracted solution is rotated. Three orthogonal rotation methods are available: <code>VARIMAX</code> (which is the default), <code>EQUAMAX</code>, and <code>QUARTIMAX</code>. There is one oblique rotation method, <em>viz</em> : <code>PROMAX</code>. Optionally you may enter the power of the promax rotation k, which must be enclosed in parentheses. The default value of k is 5. If you don’t want any rotation to be performed, the word <code>NOROTATE</code> prevents the command from performing any rotation on the data.</p><p>The <code>/METHOD</code> subcommand should be used to determine whether the covariance matrix or the correlation matrix of the data is to be analysed. By default, the correlation matrix is analysed.</p><p>The <code>/PRINT</code> subcommand may be used to select which features of the analysis are reported:</p><ul><li><code>UNIVARIATE</code> A table of mean values, standard deviations and total weights are printed.</li><li><code>INITIAL</code> Initial communalities and eigenvalues are printed.</li><li><code>EXTRACTION</code> Extracted communalities and eigenvalues are printed.</li><li><code>ROTATION</code> Rotated communalities and eigenvalues are printed.</li><li><code>CORRELATION</code> The correlation matrix is printed.</li><li><code>COVARIANCE</code> The covariance matrix is printed.</li><li><code>DET</code> The determinant of the correlation or covariance matrix is printed.</li><li><code>AIC</code> The anti-image covariance and anti-image correlation matrices are printed.</li><li><code>KMO</code> The Kaiser-Meyer-Olkin measure of sampling adequacy and the Bartlett test of sphericity is printed.</li><li><code>SIG</code> The significance of the elements of correlation matrix is printed.</li><li><code>ALL</code> All of the above are printed.</li><li><code>DEFAULT</code> Identical to <code>INITIAL</code> and <code>EXTRACTION</code>.</li></ul><p>If <code>/PLOT=EIGEN</code> is given, then a “Scree” plot of the eigenvalues is printed. This can be useful for visualizing the factors and deciding which factors (components) should be retained.</p><p>The <code>/FORMAT</code> subcommand determined how data are to be displayed in loading matrices. If <code>SORT</code> is specified, then the variables are sorted in descending order of significance. If <code>BLANK(n)</code> is specified, then coefficients whose absolute value is less than n are not printed. If the keyword <code>DEFAULT</code> is specified, or if no <code>/FORMAT</code> subcommand is specified, then no sorting is performed, and all coefficients are printed.</p><p>You can use the <code>/CRITERIA</code> subcommand to specify how the number of extracted factors (components) are chosen. If <code>FACTORS(n)</code> is specified, where n is an integer, then n factors are extracted. Otherwise, the <code>MINEIGEN</code> setting is used. <code>MINEIGEN(l)</code> requests that all factors whose eigenvalues are greater than or equal to l are extracted. The default value of l is 1. The <code>ECONVERGE</code> setting has effect only when using iterative algorithms for factor extraction (such as Principal Axis Factoring). <code>ECONVERGE(delta)</code> specifies that iteration should cease when the maximum absolute value of the communality estimate between one iteration and the previous is less than delta. The default value of delta is 0.001.</p><p>The <code>ITERATE(m)</code> may appear any number of times and is used for two different purposes. It is used to set the maximum number of iterations (m) for convergence and also to set the maximum number of iterations for rotation. Whether it affects convergence or rotation depends upon which subcommand follows the <code>ITERATE</code> subcommand. If <code>EXTRACTION</code> follows, it affects convergence. If <code>ROTATION</code> follows, it affects rotation. If neither <code>ROTATION</code> nor <code>EXTRACTION</code> follow a <code>ITERATE</code> subcommand, then the entire subcommand is ignored. The default value of m is 25.</p><p>The <code>MISSING</code> subcommand determines the handling of missing variables. If <code>INCLUDE</code> is set, then user-missing values are included in the calculations, but system-missing values are not. If <code>EXCLUDE</code> is set, which is the default, user-missing values are excluded as well as system-missing values. This is the default. If <code>LISTWISE</code> is set, then the entire case is excluded from analysis whenever any variable specified in the <code>VARIABLES</code> subcommand contains a missing value.</p><p>If <code>PAIRWISE</code> is set, then a case is considered missing only if either of the values for the particular coefficient are missing. The default is <code>LISTWISE</code>.</p>`,16)])])}const T=o(n,[["render",d]]);export{m as __pageData,T as default};
