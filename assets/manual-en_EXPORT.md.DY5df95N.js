import{_ as a,o as t,c as o,a2 as s}from"./chunks/framework.Bg-Zxr5O.js";const E=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/EXPORT.md","filePath":"manual-en/EXPORT.md","lastUpdated":1785684581000}'),n={name:"manual-en/EXPORT.md"};function i(r,e,d,c,l,p){return t(),o("div",null,[...e[0]||(e[0]=[s(`<h3 id="_9-2-export" tabindex="-1">9.2 EXPORT <a class="header-anchor" href="#_9-2-export" aria-label="Permalink to &quot;9.2 EXPORT&quot;">​</a></h3><pre><code>EXPORT
        /OUTFILE=’file_name’
        /UNSELECTED={RETAIN,DELETE}
        /DIGITS=n
        /DROP=var_list
        /KEEP=var_list
        /RENAME=(src_names=target_names)…
        /TYPE={COMM,TAPE}
        /MAP
</code></pre><p>The <code>EXPORT</code> procedure writes the active dataset’s dictionary and data to a specified portable file.</p><p>By default, cases excluded with FILTER are written to the file. These can be excluded by specifying DELETE on the <code>UNSELECTED</code> subcommand. Specifying RETAIN makes the default explicit.</p><p>Portable files express real numbers in base 30. Integers are always expressed to the maximum precision needed to make them exact. Non-integers are, by default, expressed to the machine’s maximum natural precision (approximately 15 decimal digits on many machines). If many numbers require this many digits, the portable file may significantly increase in size. As an alternative, the <code>DIGITS</code> subcommand may be used to specify the number of decimal digits of precision to write. <code>DIGITS</code> applies only to non-integers.</p><p>The <code>OUTFILE</code> subcommand, which is the only required subcommand, specifies the portable file to be written as a file name string or a file handle (see <a href="./File-Handles">File Handles</a>).</p><p><code>DROP</code>, <code>KEEP</code>, and <code>RENAME</code> follow the same format as the <code>SAVE</code> procedure (see <a href="./SAVE">SAVE</a>).</p><p>The <code>TYPE</code> subcommand specifies the character set for use in the portable file. Its value is currently not used.</p><p>The <code>MAP</code> subcommand is currently ignored.</p><p><code>EXPORT</code> is a procedure. It causes the active dataset to be read.</p>`,10)])])}const h=a(n,[["render",i]]);export{E as __pageData,h as default};
