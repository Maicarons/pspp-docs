import{_ as e,o,c,a2 as d}from"./chunks/framework.Bg-Zxr5O.js";const v=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-zh/Invoking-pspp_002dconvert.md","filePath":"manual-zh/Invoking-pspp_002dconvert.md","lastUpdated":1785684581000}'),a={name:"manual-zh/Invoking-pspp_002dconvert.md"};function t(s,p,r,n,S,l){return o(),c("div",null,[...p[0]||(p[0]=[d(`<h2 id="_18-调用-pspp-convert" tabindex="-1">18 调用 <code>pspp-convert</code> <a class="header-anchor" href="#_18-调用-pspp-convert" aria-label="Permalink to &quot;18 调用 \`pspp-convert\`&quot;">​</a></h2><p><code>pspp-convert</code> 是 PSPP 附带的命令行实用程序。它读取 SPSS 或 SPSS/PC+ 系统文件、SPSS 便携文件或加密的 SPSS 语法文件作为输入，并将副本以不同格式写入另一个输出。概要：</p><pre><code>pspp-convert [options] input output

pspp-convert --help

pspp-convert --version
</code></pre><p>输入的格式在可能时自动检测。旧 SPSS 系统文件的字符编码并非总能正确猜测，而 SPSS/PC+ 系统文件不包含任何编码指示。在这种情况下，使用 <code>-e encoding</code> 指定编码。</p><p>默认情况下，输出的目标格式根据其扩展名推断：</p><p><code>csv</code><code>txt</code></p><p>逗号分隔值。每个值根据其变量的打印格式格式化。文件的第一行包含变量名。</p><p><code>sav</code><code>sys</code></p><p>SPSS 系统文件。</p><p><code>por</code></p><p>SPSS 便携文件。</p><p><code>sps</code></p><p>SPSS 语法文件。（只有加密的语法文件可以转换为此格式。）</p><p><code>pspp-convert</code> 可以将大多数输入格式转换为大多数输出格式。加密的 SPSS 文件格式是例外：如果输入文件是加密格式，则输出文件将是相同格式（已解密）。要解密此类文件，请将加密文件指定为输入。输出将是等效的纯文本文件。这种情况下输出格式的选项会被忽略。</p><p>加密文件的密码可以通过几种不同的方式指定。如果知道密码，请使用 -p 选项（如下所述）或允许 <code>pspp-convert</code> 提示输入。如果不知道密码，请使用 -a 和 -l 选项指定如何搜索它，或使用 --password-list 指定要尝试的密码文件。</p><p>使用 <code>-O format</code> 覆盖推断的格式或为无法识别的扩展名指定格式。</p><p><code>pspp-convert</code> 接受以下常规选项：</p><p>-O format --output-format=format</p><p>设置输出格式，其中 format 是上面列出的扩展名之一，<em>例如</em>：-O csv。使用 --help 列出支持的输出格式。</p><p>-c maxcases --cases=maxcases</p><p>默认情况下，所有个案都从输入复制到输出。指定此选项可将写入输出的个案数限制为 maxcases。</p><p>-e charset --encoding=charset</p><p>覆盖解释输入中字符串的编码。此选项是必要的，因为旧 SPSS 系统文件和 SPSS/PC+ 系统文件不会自行标识其编码。</p><p>-k variable… --keep=variable…</p><p>默认情况下，<code>pspp-convert</code> 包含输入文件中的所有变量。使用此选项列出要包含的特定变量；任何未列出的变量将被丢弃。输出文件中的变量也会重新排序为给定的顺序。变量列表可以像 PSPP 语法中一样使用 <code>TO</code>，<em>例如</em> 如果字典包含连续变量 <code>a</code>、<code>b</code>、<code>c</code> 和 <code>d</code>，则 --keep=&#39;a to d&#39; 将包含所有这些变量（不包含其他变量）。</p><p>-d variable… --drop=variable…</p><p>从输出中丢弃指定的变量。</p><p>当 --keep 和 --drop 一起使用时，先处理 --keep。</p><p>-h --help</p><p>在 stdout 上打印用法消息并退出。</p><p>-v --version</p><p>在 stdout 上打印版本信息并退出。</p><p>以下选项影响 CSV 输出：</p><p>--recode</p><p>默认情况下，<code>pspp-convert</code> 将用户缺失值作为常规值写入 CSV 输出文件。使用此选项时，<code>pspp-convert</code> 将它们重新编码为系统缺失值（写入为单个空格）。</p><p>--no-var-names</p><p>默认情况下，<code>pspp-convert</code> 将变量名作为输出的第一行写入。使用此选项时，<code>pspp-convert</code> 省略此行。</p><p>--labels</p><p>默认情况下，<code>pspp-convert</code> 将变量的值写入 CSV 输出文件。使用此选项时，<code>pspp-convert</code> 写入值标签。</p><p>--print-formats</p><p>默认情况下，<code>pspp-convert</code> 将数值变量作为纯数字写入。此选项使 <code>pspp-convert</code> 遵循变量的打印格式。</p><p>--decimal=decimal</p><p>此选项设置输出中用作小数点的字符。默认值为 ‘.’。</p><p>--delimiter=delimiter</p><p>此选项设置用于分隔输出中字段的字符。默认值为 ‘，’，除非小数点为 ‘，’，此时使用 ‘；’。</p><p>--qualifier=qualifier</p><p>此选项设置用于引用包含分隔符的字段的字符。默认值为 ‘&quot;’。</p><p>以下选项指定如何获取加密文件的密码：</p><p>-p password --password=password</p><p>指定用于解密加密 SPSS 系统文件或语法文件的密码。如果未指定此选项，<code>pspp-convert</code> 将在必要时交互提示输入密码。</p><p>请注意，在多用户系统上，命令行选项（包括密码）可能对其他用户可见。</p><p>与 -a（或 --password-alphabet）和 -l（或 --password-length）一起使用时，此选项指定搜索的起点。这可用于重新开始被中断的搜索。</p><p>-a alphabet --password-alphabet=alphabet</p><p>指定用于搜索加密文件密码的符号字母表。alphabet 可以包含单个字符和以 ‘-’ 分隔的范围。例如，-a a-z 搜索小写字母，-a A-Z0-9 搜索大写字母和数字，-a &#39; -~&#39; 搜索所有可打印的 ASCII 字符。</p><p>-l max-length --password-length=max-length</p><p>指定要尝试的密码的最大长度。</p><p>--password-list=file</p><p>指定要读取的包含密码列表的文件，每行一个密码。如果 file 为 -，则从 stdin 读取。</p>`,58)])])}const m=e(a,[["render",t]]);export{v as __pageData,m as default};
