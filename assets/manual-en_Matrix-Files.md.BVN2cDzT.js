import{_ as a,o,c as t,a2 as i}from"./chunks/framework.Bg-Zxr5O.js";const m=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/Matrix-Files.md","filePath":"manual-en/Matrix-Files.md","lastUpdated":1785684581000}'),r={name:"manual-en/Matrix-Files.md"};function s(c,e,d,n,p,l){return o(),t("div",null,[...e[0]||(e[0]=[i('<h3 id="_16-1-matrix-files" tabindex="-1">16.1 Matrix Files <a class="header-anchor" href="#_16-1-matrix-files" aria-label="Permalink to &quot;16.1 Matrix Files&quot;">​</a></h3><p>A matrix file is an SPSS system file that conforms to the dictionary and case structure described in this section. Procedures that read matrices from files expect them to be in the matrix file format. Procedures that write matrices also use this format.</p><p>Text files that contain matrices can be converted to matrix file format. See <a href="./MATRIX-DATA">MATRIX DATA</a>, for a command to read a text file as a matrix file.</p><p>A matrix file’s dictionary must have the following variables in the specified order:</p><ol><li>Zero or more numeric split variables. These are included by procedures when <code>SPLIT FILE</code> is active. <code>MATRIX DATA</code> assigns split variables format F4.0.</li><li><code>ROWTYPE_</code>, a string variable with width 8. This variable indicates the kind of matrix or vector that a given case represents. The supported row types are listed below.</li><li>Zero or more numeric factor variables. These are included by procedures that divide data into cells. For within-cell data, factor variables are filled with non-missing values; for pooled data, they are missing. <code>MATRIX DATA</code> assigns factor variables format F4.0.</li><li><code>VARNAME_</code>, a string variable. Matrix data includes one row per continuous variable (see below), naming each continuous variable in order. This column is blank for vector data. <code>MATRIX DATA</code> makes <code>VARNAME_</code> wide enough for the name of any of the continuous variables, but at least 8 bytes.</li><li>One or more numeric continuous variables. These are the variables whose data was analyzed to produce the matrices. <code>MATRIX DATA</code> assigns continuous variables format F10.4.</li></ol><p>Case weights are ignored in matrix files.</p><h4 id="row-types" tabindex="-1">Row Types <a class="header-anchor" href="#row-types" aria-label="Permalink to &quot;Row Types&quot;">​</a></h4><p>Matrix files support a fixed set of types of matrix and vector data. The <code>ROWTYPE_</code> variable in each case of a matrix file indicates its row type.</p><p>The supported matrix row types are listed below. Each type is listed with the keyword that identifies it in <code>ROWTYPE_</code>. All supported types of matrices are square, meaning that each matrix must include one row per continuous variable, with the <code>VARNAME_</code> variable indicating each continuous variable in turn in the same order as the dictionary.</p><p><code>CORR</code></p><p>Correlation coefficients.</p><p><code>COV</code></p><p>Covariance coefficients.</p><p><code>MAT</code></p><p>General-purpose matrix.</p><p><code>N_MATRIX</code></p><p>Counts.</p><p><code>PROX</code></p><p>Proximities matrix.</p><p>The supported vector row types are listed below, along with their associated keyword. Vector row types only require a single row, whose <code>VARNAME_</code> is blank:</p><p><code>COUNT</code></p><p>Unweighted counts.</p><p><code>DFE</code></p><p>Degrees of freedom.</p><p><code>MEAN</code></p><p>Means.</p><p><code>MSE</code></p><p>Mean squared errors.</p><p><code>N</code></p><p>Counts.</p><p><code>STDDEV</code></p><p>Standard deviations.</p><p>Only the row types listed above may appear in matrix files. The <code>MATRIX DATA</code> command, however, accepts the additional row types listed below, which it changes into matrix file row types as part of its conversion process:</p><p><code>N_VECTOR</code></p><p>Synonym for <code>N</code>.</p><p><code>SD</code></p><p>Synonym for <code>STDDEV</code>.</p><p><code>N_SCALAR</code></p><p>Accepts a single number from the <code>MATRIX DATA</code> input and writes it as an <code>N</code> row with the number replicated across all the continuous variables.</p>',39)])])}const u=a(r,[["render",s]]);export{m as __pageData,u as default};
