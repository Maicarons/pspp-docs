import{_ as t,o as n,c as a,a2 as i}from"./chunks/framework.Bg-Zxr5O.js";const m=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/FILE-HANDLE.md","filePath":"manual-en/FILE-HANDLE.md","lastUpdated":1785684581000}'),o={name:"manual-en/FILE-HANDLE.md"};function s(r,e,d,l,c,h){return n(),a("div",null,[...e[0]||(e[0]=[i(`<h3 id="_8-8-file-handle" tabindex="-1">8.8 FILE HANDLE <a class="header-anchor" href="#_8-8-file-handle" aria-label="Permalink to &quot;8.8 FILE HANDLE&quot;">​</a></h3><pre><code>For text files:
        FILE HANDLE handle_name
                /NAME=’file_name
                [/MODE=CHARACTER]
                [/ENDS={CR,CRLF}]
                /TABWIDTH=tab_width
                [ENCODING=’encoding’]

For binary files in native encoding with fixed-length records:
        FILE HANDLE handle_name
                /NAME=’file_name’
                /MODE=IMAGE
                [/LRECL=rec_len]
                [ENCODING=’encoding’]

For binary files in native encoding with variable-length records:
        FILE HANDLE handle_name
                /NAME=’file_name’
                /MODE=BINARY
                [/LRECL=rec_len]
                [ENCODING=’encoding’]

For binary files encoded in EBCDIC:
        FILE HANDLE handle_name
                /NAME=’file_name’
                /MODE=360
                /RECFORM={FIXED,VARIABLE,SPANNED}
                [/LRECL=rec_len]
                [ENCODING=’encoding’]
</code></pre><p>Use <code>FILE HANDLE</code> to associate a file handle name with a file and its attributes, so that later commands can refer to the file by its handle name. Names of text files can be specified directly on commands that access files, so that <code>FILE HANDLE</code> is only needed when a file is not an ordinary file containing lines of text. However, <code>FILE HANDLE</code> may be used even for text files, and it may be easier to specify a file’s name once and later refer to it by an abstract handle.</p><p>Specify the file handle name as the identifier immediately following the <code>FILE HANDLE</code> command name. The identifier INLINE is reserved for representing data embedded in the syntax file (see <a href="./BEGIN-DATA">BEGIN DATA</a>) The file handle name must not already have been used in a previous invocation of <code>FILE HANDLE</code>, unless it has been closed by an intervening command (see <a href="./CLOSE-FILE-HANDLE">CLOSE FILE HANDLE</a>).</p><p>The effect and syntax of <code>FILE HANDLE</code> depends on the selected MODE:</p><ul><li>In CHARACTER mode, the default, the data file is read as a text file. Each text line is read as one record.</li></ul><p>In CHARACTER mode only, tabs are expanded to spaces by input programs, except by <code>DATA LIST FREE</code> with explicitly specified delimiters. Each tab is 4 characters wide by default, but TABWIDTH (a PSPP extension) may be used to specify an alternate width. Use a TABWIDTH of 0 to suppress tab expansion.</p><p>A file written in CHARACTER mode by default uses the line ends of the system on which PSPP is running, that is, on Windows, the default is CR LF line ends, and on other systems the default is LF only. Specify ENDS as CR or CRLF to override the default. PSPP reads files using either convention on any kind of system, regardless of ENDS.</p><ul><li>In IMAGE mode, the data file is treated as a series of fixed-length binary records. LRECL should be used to specify the record length in bytes, with a default of 1024. On input, it is an error if an IMAGE file’s length is not an integer multiple of the record length. On output, each record is padded with spaces or truncated, if necessary, to make it exactly the correct length.</li><li>In BINARY mode, the data file is treated as a series of variable-length binary records. LRECL may be specified, but its value is ignored. The data for each record is both preceded and followed by a 32-bit signed integer in little-endian byte order that specifies the length of the record. (This redundancy permits records in these files to be efficiently read in reverse order, although PSPP always reads them in forward order.) The length does not include either integer.</li><li>Mode 360 reads and writes files in formats first used for tapes in the 1960s on IBM mainframe operating systems and still supported today by the modern successors of those operating systems. For more information, see OS/400 Tape and Diskette Device Programming, available on IBM’s website.</li></ul><p>Alphanumeric data in mode 360 files are encoded in EBCDIC. PSPP translates EBCDIC to or from the host’s native format as necessary on input or output, using an ASCII/EBCDIC translation that is one-to-one, so that a “round trip” from ASCII to EBCDIC back to ASCII, or vice versa, always yields exactly the original data.</p><p>The <code>RECFORM</code> subcommand is required in mode 360. The precise file format depends on its setting:</p><p>F FIXED</p><p>This record format is equivalent to IMAGE mode, except for EBCDIC translation.</p><p>IBM documentation calls this <code>*F</code> (fixed-length, deblocked) format.</p><p>V VARIABLE</p><p>The file comprises a sequence of zero or more variable-length blocks. Each block begins with a 4-byte <em>block descriptor word</em> (BDW). The first two bytes of the BDW are an unsigned integer in big-endian byte order that specifies the length of the block, including the BDW itself. The other two bytes of the BDW are ignored on input and written as zeros on output.</p><p>Following the BDW, the remainder of each block is a sequence of one or more variable-length records, each of which in turn begins with a 4-byte <em>record descriptor word</em> (RDW) that has the same format as the BDW. Following the RDW, the remainder of each record is the record data.</p><p>The maximum length of a record in VARIABLE mode is 65,527 bytes: 65,535 bytes (the maximum value of a 16-bit unsigned integer), minus 4 bytes for the BDW, minus 4 bytes for the RDW.</p><p>In mode VARIABLE, LRECL specifies a maximum, not a fixed, record length, in bytes. The default is 8,192.</p><p>IBM documentation calls this <code>*VB</code> (variable-length, blocked, unspanned) format.</p><p>VS SPANNED</p><p>The file format is like that of VARIABLE mode, except that logical records may be split among multiple physical records (called <em>segments</em>) or blocks. In SPANNED mode, the third byte of each RDW is called the segment control character (SCC). Odd SCC values cause the segment to be appended to a record buffer maintained in memory; even values also append the segment and then flush its contents to the input procedure. Canonically, SCC value 0 designates a record not spanned among multiple segments, and values 1 through 3 designate the first segment, the last segment, or an intermediate segment, respectively, within a multi-segment record. The record buffer is also flushed at end of file regardless of the final record’s SCC.</p><p>The maximum length of a logical record in VARIABLE mode is limited only by memory available to PSPP. Segments are limited to 65,527 bytes, as in VARIABLE mode.</p><p>This format is similar to what IBM documentation call <code>*VS</code> (variable-length, deblocked, spanned) format.</p><p>In mode 360, fields of type A that extend beyond the end of a record read from disk are padded with spaces in the host’s native character set, which are then translated from EBCDIC to the native character set. Thus, when the host’s native character set is based on ASCII, these fields are effectively padded with character <code>X&#39;80&#39;</code>. This wart is implemented for compatibility.</p><p>The <code>NAME</code> subcommand specifies the name of the file associated with the handle. It is required in all modes but SCRATCH mode, in which its use is forbidden.</p><p>The ENCODING subcommand specifies the encoding of text in the file. For reading text files in CHARACTER mode, all of the forms described for ENCODING on the INSERT command are supported (see <a href="./INSERT">INSERT</a>). For reading in other file-based modes, encoding autodetection is not supported; if the specified encoding requests autodetection then the default encoding is used. This is also true when a file handle is used for writing a file in any mode.</p>`,27)])])}const p=t(o,[["render",s]]);export{m as __pageData,p as default};
