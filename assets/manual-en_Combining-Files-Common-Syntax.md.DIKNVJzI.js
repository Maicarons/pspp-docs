import{_ as a,o as t,c as o,a2 as i}from"./chunks/framework.Bg-Zxr5O.js";const f=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/Combining-Files-Common-Syntax.md","filePath":"manual-en/Combining-Files-Common-Syntax.md","lastUpdated":1785684581000}'),n={name:"manual-en/Combining-Files-Common-Syntax.md"};function s(c,e,d,r,l,h){return t(),o("div",null,[...e[0]||(e[0]=[i(`<h3 id="_10-1-common-syntax" tabindex="-1">10.1 Common Syntax <a class="header-anchor" href="#_10-1-common-syntax" aria-label="Permalink to &quot;10.1 Common Syntax&quot;">​</a></h3><pre><code>Per input file:
        /FILE={*,’file_name’}
        [/RENAME=(src_names=target_names)…]
        [/IN=var_name]
        [/SORT]

Once per command:
        /BY var_list[({D|A})] [var_list[({D|A}]]…
        [/DROP=var_list]
        [/KEEP=var_list]
        [/FIRST=var_name]
        [/LAST=var_name]
        [/MAP]
</code></pre><p>This section describes the syntactical features in common among the <code>ADD FILES</code>, <code>MATCH FILES</code>, and <code>UPDATE</code> commands. The following sections describe details specific to each command.</p><p>Each of these commands reads two or more input files and combines them. The command’s output becomes the new active dataset. None of the commands actually change the input files. Therefore, if you want the changes to become permanent, you must explicitly save them using an appropriate procedure or transformation (see <a href="./System-and-Portable-File-IO">System and Portable File IO</a>).</p><p>The syntax of each command begins with a specification of the files to be read as input. For each input file, specify FILE with a system file or portable file’s name as a string, a dataset (see <a href="./Datasets">Datasets</a>) or file handle name, (see <a href="./File-Handles">File Handles</a>), or an asterisk (‘*’) to use the active dataset as input. Use of portable files on <code>FILE</code> is a PSPP extension.</p><p>At least two <code>FILE</code> subcommands must be specified. If the active dataset is used as an input source, then <code>TEMPORARY</code> must not be in effect.</p><p>Each <code>FILE</code> subcommand may be followed by any number of <code>RENAME</code> subcommands that specify a parenthesized group or groups of variable names as they appear in the input file, followed by those variables’ new names, separated by an equals sign (<code>=</code>), <em>e.g.</em> <code>/RENAME=(OLD1=NEW1)(OLD2=NEW2)</code>. To rename a single variable, the parentheses may be omitted: <code>/RENAME=old=new</code>. Within a parenthesized group, variables are renamed simultaneously, so that <code>/RENAME=(A B=B A)</code> exchanges the names of variables A and B. Otherwise, renaming occurs in left-to-right order.</p><p>Each <code>FILE</code> subcommand may optionally be followed by a single <code>IN</code> subcommand, which creates a numeric variable with the specified name and format F1.0. The IN variable takes value 1 in an output case if the given input file contributed to that output case, and 0 otherwise. The <code>DROP</code>, <code>KEEP</code>, and <code>RENAME</code> subcommands have no effect on IN variables.</p><p>If <code>BY</code> is used (see below), the <code>SORT</code> keyword must be specified after a <code>FILE</code> if that input file is not already sorted on the <code>BY</code> variables. When <code>SORT</code> is specified, PSPP sorts the input file’s data on the <code>BY</code> variables before it applies it to the command. When <code>SORT</code> is used, <code>BY</code> is required. <code>SORT</code> is a PSPP extension.</p><p>PSPP merges the dictionaries of all of the input files to form the dictionary of the new active dataset, like so:</p><ul><li>The variables in the new active dataset are the union of all the input files’ variables, matched based on their name. When a single input file contains a variable with a given name, the output file will contain exactly that variable. When more than one input file contains a variable with a given name, those variables must be all string or all numeric. If they are string variables, then the result will have the width of the longest variable with that name, with narrower values padded on the right with spaces to fill the width. Variables are matched after renaming with the <code>RENAME</code> subcommand. Thus, <code>RENAME</code> can be used to resolve conflicts. Only variables in the output file can conflict, so <code>DROP</code> or <code>KEEP</code>, as described below, can also resolve a conflict.</li><li>The variable label for each output variable is taken from the first specified input file that has a variable label for that variable, and similarly for value labels and missing values.</li><li>The file label of the new active dataset (see <a href="./FILE-LABEL">FILE LABEL</a>) is that of the first specified <code>FILE</code> that has a file label.</li><li>The documents in the new active dataset (see <a href="./DOCUMENT">DOCUMENT</a>) are the concatenation of all the input files’ documents, in the order in which the <code>FILE</code> subcommands are specified.</li><li>If all of the input files are weighted on the same variable, then the new active dataset is weighted on that variable. Otherwise, the new active dataset is not weighted.</li></ul><p>The remaining subcommands apply to the output file as a whole, rather than to individual input files. They must be specified at the end of the command specification, following all of the <code>FILE</code> and related subcommands. The most important of these subcommands is <code>BY</code>, which specifies a set of one or more variables that may be used to find corresponding cases in each of the input files. The variables specified on <code>BY</code> must be present in all of the input files. Furthermore, if any of the input files are not sorted on the <code>BY</code> variables, then <code>SORT</code> must be specified for those input files.</p><p>The variables listed on <code>BY</code> may include (A) or (D) annotations to specify ascending or descending sort order. See <a href="./SORT-CASES">SORT CASES</a>, for more details on this notation. Adding (A) or (D) to the <code>BY</code> subcommand specification is a PSPP extension.</p><p>The <code>DROP</code> subcommand can be used to specify a list of variables to exclude from the output. By contrast, the <code>KEEP</code> subcommand can be used to specify variables to include in the output; all variables not listed are dropped. <code>DROP</code> and <code>KEEP</code> are executed in left-to-right order and may be repeated any number of times. <code>DROP</code> and <code>KEEP</code> do not affect variables created by the <code>IN</code>, <code>FIRST</code>, and <code>LAST</code> subcommands, which are always included in the new active dataset, but they can be used to drop <code>BY</code> variables.</p><p>The <code>FIRST</code> and <code>LAST</code> subcommands are optional. They may only be specified on <code>MATCH FILES</code> and <code>ADD FILES</code>, and only when <code>BY</code> is used. <code>FIRST</code> and <code>LIST</code> each adds a numeric variable to the new active dataset, with the name given as the subcommand’s argument and F1.0 print and write formats. The value of the <code>FIRST</code> variable is 1 in the first output case with a given set of values for the <code>BY</code> variables, and 0 in other cases. Similarly, the <code>LAST</code> variable is 1 in the last case with a given of <code>BY</code> values, and 0 in other cases.</p><p>When any of these commands creates an output case, variables that are only in files that are not present for the current case are set to the system-missing value for numeric variables or spaces for string variables.</p><p>These commands may combine any number of files, limited only by the machine’s memory.</p>`,17)])])}const u=a(n,[["render",s]]);export{f as __pageData,u as default};
