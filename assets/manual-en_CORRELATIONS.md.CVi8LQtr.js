import{_ as s,o as a,c as o,a2 as i}from"./chunks/framework.Bg-Zxr5O.js";const f=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/CORRELATIONS.md","filePath":"manual-en/CORRELATIONS.md","lastUpdated":1785684581000}'),t={name:"manual-en/CORRELATIONS.md"};function d(n,e,c,r,l,I){return a(),o("div",null,[...e[0]||(e[0]=[i(`<h3 id="_15-5-correlations" tabindex="-1">15.5 CORRELATIONS <a class="header-anchor" href="#_15-5-correlations" aria-label="Permalink to &quot;15.5 CORRELATIONS&quot;">​</a></h3><pre><code>CORRELATIONS
     /VARIABLES = var_list [ WITH var_list ]
     [
      .
      .
      .
      /VARIABLES = var_list [ WITH var_list ]
      /VARIABLES = var_list [ WITH var_list ]
     ]

     [ /PRINT={TWOTAIL, ONETAIL} {SIG, NOSIG} ]
     [ /STATISTICS=DESCRIPTIVES XPROD ALL]
     [ /MISSING={PAIRWISE, LISTWISE} {INCLUDE, EXCLUDE} ]
</code></pre><p>The <code>CORRELATIONS</code> procedure produces tables of the Pearson correlation coefficient for a set of variables. The significance of the coefficients are also given.</p><p>At least one <code>VARIABLES</code> subcommand is required. If you specify the <code>WITH</code> keyword, then a non-square correlation table is produced. The variables preceding <code>WITH</code>, are used as the rows of the table, and the variables following <code>WITH</code> are used as the columns of the table. If no <code>WITH</code> subcommand is specified, then <code>CORRELATIONS</code> produces a square, symmetrical table using all variables.</p><p>The <code>MISSING</code> subcommand determines the handling of missing variables. If <code>INCLUDE</code> is set, then user-missing values are included in the calculations, but system-missing values are not. If <code>EXCLUDE</code> is set, which is the default, user-missing values are excluded as well as system-missing values.</p><p>If <code>LISTWISE</code> is set, then the entire case is excluded from analysis whenever any variable specified in any <code>/VARIABLES</code> subcommand contains a missing value. If <code>PAIRWISE</code> is set, then a case is considered missing only if either of the values for the particular coefficient are missing. The default is <code>PAIRWISE</code>.</p><p>The <code>PRINT</code> subcommand is used to control how the reported significance values are printed. If the <code>TWOTAIL</code> option is used, then a two-tailed test of significance is printed. If the <code>ONETAIL</code> option is given, then a one-tailed test is used. The default is <code>TWOTAIL</code>.</p><p>If the <code>NOSIG</code> option is specified, then correlation coefficients with significance less than 0.05 are highlighted. If <code>SIG</code> is specified, then no highlighting is performed. This is the default.</p><p>The <code>STATISTICS</code> subcommand requests additional statistics to be displayed. The keyword <code>DESCRIPTIVES</code> requests that the mean, number of non-missing cases, and the non-biased estimator of the standard deviation are displayed. These statistics are displayed in a separated table, for all the variables listed in any <code>/VARIABLES</code> subcommand. The <code>XPROD</code> keyword requests cross-product deviations and covariance estimators to be displayed for each pair of variables. The keyword <code>ALL</code> is the union of <code>DESCRIPTIVES</code> and <code>XPROD</code>.</p>`,9)])])}const u=s(t,[["render",d]]);export{f as __pageData,u as default};
