import{_ as a,o,c as n,a2 as c}from"./chunks/framework.Bg-Zxr5O.js";const h=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/Controlling-Macro-Expansion.md","filePath":"manual-en/Controlling-Macro-Expansion.md","lastUpdated":1785684581000}'),t={name:"manual-en/Controlling-Macro-Expansion.md"};function r(d,e,s,l,i,p){return o(),n("div",null,[...e[0]||(e[0]=[c('<h4 id="_14-2-5-controlling-macro-expansion" tabindex="-1">14.2.5 Controlling Macro Expansion <a class="header-anchor" href="#_14-2-5-controlling-macro-expansion" aria-label="Permalink to &quot;14.2.5 Controlling Macro Expansion&quot;">​</a></h4><p>Multiple factors control whether macro calls are expanded in different situations. At the highest level, <code>SET MEXPAND</code> controls whether macro calls are expanded. By default, it is enabled. See <a href="./SET#SET-MEXPAND">SET MEXPAND</a>, for details.</p><p>A macro body may contain macro calls. By default, these are expanded. If a macro body contains <code>!OFFEXPAND</code> or <code>!ONEXPAND</code> directives, then <code>!OFFEXPAND</code> disables expansion of macro calls until the following <code>!ONEXPAND</code>.</p><p>A macro argument’s value may contain a macro call. These macro calls are expanded, unless the argument was declared with the <code>!NOEXPAND</code> keyword.</p><p>The argument to a macro function is a special context that does not expand macro calls. For example, if <code>!vars</code> is the name of a macro, then <code>!LENGTH(!vars)</code> expands to 5, as does <code>!LENGTH(!1)</code> if positional argument 1 has value <code>!vars</code>. To expand macros in these cases, use the <code>!EVAL</code> macro function, e.g. <code>!LENGTH(!EVAL(!vars))</code> or <code>!LENGTH(!EVAL(!1))</code>. See <a href="./Macro-Functions">Macro Functions</a>, for details.</p><p>These rules apply to macro calls, not to uses within a macro body of macro functions, macro arguments, and macro variables created by <code>!DO</code> or <code>!LET</code>, which are always expanded.</p><p><code>SET MEXPAND</code> may appear within the body of a macro, but it will not affect expansion of the macro that it appears in. Use <code>!OFFEXPAND</code> and <code>!ONEXPAND</code> instead.</p>',7)])])}const f=a(t,[["render",r]]);export{h as __pageData,f as default};
