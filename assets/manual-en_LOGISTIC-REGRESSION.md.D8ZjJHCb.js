import{_ as i,o as t,c as o,a2 as a}from"./chunks/framework.Bg-Zxr5O.js";const u=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/LOGISTIC-REGRESSION.md","filePath":"manual-en/LOGISTIC-REGRESSION.md","lastUpdated":1785684581000}'),s={name:"manual-en/LOGISTIC-REGRESSION.md"};function n(d,e,r,l,c,h){return t(),o("div",null,[...e[0]||(e[0]=[a(`<h3 id="_15-10-logistic-regression" tabindex="-1">15.10 LOGISTIC REGRESSION <a class="header-anchor" href="#_15-10-logistic-regression" aria-label="Permalink to &quot;15.10 LOGISTIC REGRESSION&quot;">​</a></h3><pre><code>LOGISTIC REGRESSION [VARIABLES =] dependent_var WITH predictors

     [/CATEGORICAL = categorical_predictors]

     [{/NOCONST | /ORIGIN | /NOORIGIN }]

     [/PRINT = [SUMMARY] [DEFAULT] [CI(confidence)] [ALL]]

     [/CRITERIA = [BCON(min_delta)] [ITERATE(max_interations)]
                  [LCON(min_likelihood_delta)] [EPS(min_epsilon)]
                  [CUT(cut_point)]]

     [/MISSING = {INCLUDE|EXCLUDE}]
</code></pre><p>Bivariate Logistic Regression is used when you want to explain a dichotomous dependent variable in terms of one or more predictor variables.</p><p>The minimum command is</p><pre><code>LOGISTIC REGRESSION y WITH x1 x2 … xn.
</code></pre><p>Here, y is the dependent variable, which must be dichotomous and x1 … xn are the predictor variables whose coefficients the procedure estimates.</p><p>By default, a constant term is included in the model. Hence, the full model is <em>{\\bf y} = b_0 + b_1 {\\bf x_1} + b_2 {\\bf x_2} + \\dots + b_n {\\bf x_n}</em></p><p>Predictor variables which are categorical in nature should be listed on the <code>/CATEGORICAL</code> subcommand. Simple variables as well as interactions between variables may be listed here.</p><p>If you want a model without the constant term <em>b_0</em> , use the keyword <code>/ORIGIN</code>. <code>/NOCONST</code> is a synonym for <code>/ORIGIN</code>.</p><p>An iterative Newton-Raphson procedure is used to fit the model. The <code>/CRITERIA</code> subcommand is used to specify the stopping criteria of the procedure, and other parameters. The value of cut_point is used in the classification table. It is the threshold above which predicted values are considered to be 1. Values of cut_point must lie in the range [0,1]. During iterations, if any one of the stopping criteria are satisfied, the procedure is considered complete. The stopping criteria are:</p><ul><li>The number of iterations exceeds max_iterations. The default value of max_iterations is 20.</li><li>The change in the all coefficient estimates are less than min_delta. The default value of min_delta is 0.001.</li><li>The magnitude of change in the likelihood estimate is less than min_likelihood_delta. The default value of min_delta is zero. This means that this criterion is disabled.</li><li>The differential of the estimated probability for all cases is less than min_epsilon. In other words, the probabilities are close to zero or one. The default value of min_epsilon is 0.00000001.</li></ul><p>The <code>PRINT</code> subcommand controls the display of optional statistics. Currently there is one such option, <code>CI</code>, which indicates that the confidence interval of the odds ratio should be displayed as well as its value. <code>CI</code> should be followed by an integer in parentheses, to indicate the confidence level of the desired confidence interval.</p><p>The <code>MISSING</code> subcommand determines the handling of missing variables. If <code>INCLUDE</code> is set, then user-missing values are included in the calculations, but system-missing values are not. If <code>EXCLUDE</code> is set, which is the default, user-missing values are excluded as well as system-missing values. This is the default.</p>`,13)])])}const p=i(s,[["render",n]]);export{u as __pageData,p as default};
