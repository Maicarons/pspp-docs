import{_ as t,o,c as a,a2 as s}from"./chunks/framework.Bg-Zxr5O.js";const h=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/HOST.md","filePath":"manual-en/HOST.md","lastUpdated":1785684581000}'),n={name:"manual-en/HOST.md"};function r(d,e,c,i,l,m){return o(),a("div",null,[...e[0]||(e[0]=[s(`<h3 id="_17-14-host" tabindex="-1">17.14 HOST <a class="header-anchor" href="#_17-14-host" aria-label="Permalink to &quot;17.14 HOST&quot;">​</a></h3><p>In the syntax below, the square brackets must be included in the command syntax and do not indicate that that their contents are optional.</p><pre><code>HOST COMMAND=[’command’...]
     TIMELIMIT=secs.
</code></pre><p><code>HOST</code> executes one or more commands, each provided as a string in the required <code>COMMAND</code> subcommand, in the shell of the underlying operating system. PSPP runs each command in a separate shell process and waits for it to finish before running the next one. If a command fails (with a nonzero exit status, or because it is killed by a signal), then PSPP does not run any remaining commands.</p><p>PSPP provides /dev/null as the shell’s standard input. If a process needs to read from stdin, redirect from a file or device, or use a pipe.</p><p>PSPP displays the shell’s standard output and standard error as PSPP output. Redirect to a file or <code>/dev/null</code> or another device if this is not desired.</p><p>The following example runs <code>rsync</code> to copy a file from a remote server to the local file data.txt, writing <code>rsync</code>’s own output to rsync-log.txt. PSPP displays the command’s error output, if any. If <code>rsync</code> needs to prompt the user (<em>e.g.</em> to obtain a password), the command fails. Only if the <code>rsync</code> succeeds, PSPP then runs the <code>sha512sum</code> command.</p><pre><code>HOST COMMAND=[&#39;rsync remote:data.txt data.txt &gt; rsync-log.txt&#39;
              &#39;sha512sum -c data.txt.sha512sum].
</code></pre><p>By default, PSPP waits as long as necessary for the series of commands to complete. Use the optional <code>TIMELIMIT</code> subcommand to limit the execution time to the specified number of seconds.</p><p>PSPP built for mingw does not support all the features of <code>HOST</code>.</p><p>PSPP rejects this command if the SAFER (see <a href="./SET">SET</a>) setting is active.</p>`,11)])])}const u=t(n,[["render",r]]);export{h as __pageData,u as default};
