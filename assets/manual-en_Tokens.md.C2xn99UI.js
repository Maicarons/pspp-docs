import{_ as t,o as a,c as o,a2 as s}from"./chunks/framework.Bg-Zxr5O.js";const m=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/Tokens.md","filePath":"manual-en/Tokens.md","lastUpdated":1785684581000}'),n={name:"manual-en/Tokens.md"};function i(r,e,d,c,p,l){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h3 id="_6-1-tokens" tabindex="-1">6.1 Tokens <a class="header-anchor" href="#_6-1-tokens" aria-label="Permalink to &quot;6.1 Tokens&quot;">​</a></h3><p>PSPP divides most syntax file lines into series of short chunks called <em>tokens</em>. Tokens are then grouped to form commands, each of which tells PSPP to take some action—read in data, write out data, perform a statistical procedure, etc. Each type of token is described below.</p><p><strong>Identifiers</strong></p><p>Identifiers are names that typically specify variables, commands, or subcommands. The first character in an identifier must be a letter, ‘#’, or ‘@’. The remaining characters in the identifier must be letters, digits, or one of the following special characters:</p><p>. _ $ # @</p><p>Identifiers may be any length, but only the first 64 bytes are significant. Identifiers are not case-sensitive: <code>foobar</code>, <code>Foobar</code>, <code>FooBar</code>, <code>FOOBAR</code>, and <code>FoObaR</code> are different representations of the same identifier.</p><p>Some identifiers are reserved. Reserved identifiers may not be used in any context besides those explicitly described in this manual. The reserved identifiers are:</p><p>ALL AND BY EQ GE GT LE LT NE NOT OR TO WITH</p><p><strong>Keywords</strong></p><p>Keywords are a subclass of identifiers that form a fixed part of command syntax. For example, command and subcommand names are keywords. Keywords may be abbreviated to their first 3 characters if this abbreviation is unambiguous. (Unique abbreviations of 3 or more characters are also accepted: ‘FRE’, ‘FREQ’, and ‘FREQUENCIES’ are equivalent when the last is a keyword.)</p><p>Reserved identifiers are always used as keywords. Other identifiers may be used both as keywords and as user-defined identifiers, such as variable names.</p><p><strong>Numbers</strong></p><p>Numbers are expressed in decimal. A decimal point is optional. Numbers may be expressed in scientific notation by adding ‘e’ and a base-10 exponent, so that ‘1.234e3’ has the value 1234. Here are some more examples of valid numbers:</p><pre><code>-5  3.14159265359  1e100  -.707  8945.
</code></pre><p>Negative numbers are expressed with a ‘-’ prefix. However, in situations where a literal ‘-’ token is expected, what appears to be a negative number is treated as ‘-’ followed by a positive number.</p><p>No white space is allowed within a number token, except for horizontal white space between ‘-’ and the rest of the number.</p><p>The last example above, ‘8945.’ is interpreted as two tokens, ‘8945’ and ‘.’, if it is the last token on a line. See <a href="./Commands">Forming commands of tokens</a>.</p><p><strong>Strings</strong></p><p>Strings are literal sequences of characters enclosed in pairs of single quotes (‘&#39;’) or double quotes (‘&quot;’). To include the character used for quoting in the string, double it, <em>e.g.</em> ‘&#39;it&#39;&#39;s an apostrophe&#39;’. White space and case of letters are significant inside strings.</p><p>Strings can be concatenated using ‘+’, so that ‘&quot;a&quot; + &#39;b&#39; + &#39;c&#39;’ is equivalent to ‘&#39;abc&#39;’. So that a long string may be broken across lines, a line break may precede or follow, or both precede and follow, the ‘+’. (However, an entirely blank line preceding or following the ‘+’ is interpreted as ending the current command.)</p><p>Strings may also be expressed as hexadecimal character values by prefixing the initial quote character by ‘x’ or ‘X’. Regardless of the syntax file or active dataset’s encoding, the hexadecimal digits in the string are interpreted as Unicode characters in UTF-8 encoding.</p><p>Individual Unicode code points may also be expressed by specifying the hexadecimal code point number in single or double quotes preceded by ‘u’ or ‘U’. For example, Unicode code point U+1D11E, the musical G clef character, could be expressed as <code>U&#39;1D11E&#39;</code>. Invalid Unicode code points (above U+10FFFF or in between U+D800 and U+DFFF) are not allowed.</p><p>When strings are concatenated with ‘+’, each segment’s prefix is considered individually. For example, <code>&#39;The G clef symbol is:&#39; + u&quot;1d11e&quot; + &quot;.&quot;</code> inserts a G clef symbol in the middle of an otherwise plain text string.</p><p><strong>Punctuators and Operators</strong></p><p>These tokens are the punctuators and operators:</p><p>, / = ( ) + - * / ** &lt; &lt;= &lt;&gt; &gt; &gt;= ~= &amp; | .</p><p>Most of these appear within the syntax of commands, but the period (‘.’) punctuator is used only at the end of a command. It is a punctuator only as the last character on a line (except white space). When it is the last non-space character on a line, a period is not treated as part of another token, even if it would otherwise be part of, <em>e.g.</em> , an identifier or a floating-point number.</p>`,27)])])}const u=t(n,[["render",i]]);export{m as __pageData,u as default};
