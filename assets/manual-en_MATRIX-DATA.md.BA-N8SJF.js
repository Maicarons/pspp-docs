import{_ as t,o as a,c as o,a2 as i}from"./chunks/framework.Bg-Zxr5O.js";const m=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/MATRIX-DATA.md","filePath":"manual-en/MATRIX-DATA.md","lastUpdated":1785684581000}'),n={name:"manual-en/MATRIX-DATA.md"};function d(r,e,s,c,l,p){return a(),o("div",null,[...e[0]||(e[0]=[i(`<h3 id="_16-2-matrix-data" tabindex="-1">16.2 MATRIX DATA <a class="header-anchor" href="#_16-2-matrix-data" aria-label="Permalink to &quot;16.2 MATRIX DATA&quot;">​</a></h3><pre><code>MATRIX DATA
        VARIABLES=variables
        [FILE={’file_name’ | INLINE}
        [/FORMAT=[{LIST | FREE}]
                 [{UPPER | LOWER | FULL}]
                 [{DIAGONAL | NODIAGONAL}]]
        [/SPLIT=split_vars]
        [/FACTORS=factor_vars]
        [/N=n]

The following subcommands are only needed when ROWTYPE_ is not
specified on the VARIABLES subcommand:
        [/CONTENTS={CORR,COUNT,COV,DFE,MAT,MEAN,MSE,
                    N_MATRIX,N|N_VECTOR,N_SCALAR,PROX,SD|STDDEV}]
        [/CELLS=n_cells]
</code></pre><p>The <code>MATRIX DATA</code> command convert matrices and vectors from text format into the matrix file format (See <a href="./Matrix-Files">Matrix Files</a>) for use by procedures that read matrices. It reads a text file or inline data and outputs to the active file, replacing any data already in the active dataset. The matrix file may then be used by other commands directly from the active file, or it may be written to a .sav file using the <code>SAVE</code> command.</p><p>The text data read by <code>MATRIX DATA</code> can be delimited by spaces or commas. A plus or minus sign, except immediately following a ‘d’ or ‘e’, also begins a new value. Optionally, values may be enclosed in single or double quotes.</p><p><code>MATRIX DATA</code> can read the types of matrix and vector data supported in matrix files (see <a href="./Matrix-Files#Matrix-File-Row-Types">Matrix File Row Types</a>).</p><p>The <code>FILE</code> subcommand specifies the source of the command’s input. To read input from a text file, specify its name in quotes. To supply input inline, omit <code>FILE</code> or specify <code>INLINE</code>. Inline data must directly follow <code>MATRIX DATA</code>, inside <code>BEGIN DATA</code> (see <a href="./BEGIN-DATA">BEGIN DATA</a>).</p><p><code>VARIABLES</code> is the only required subcommand. It names the variables present in each input record in the order that they appear. (<code>MATRIX DATA</code> reorders the variables in the matrix file it produces, if needed to fit the matrix file format.) The variable list must include split variables and factor variables, if they are present in the data, in addition to the continuous variables that form matrix rows and columns. It may also include a special variable named <code>ROWTYPE_</code>.</p><p>Matrix data may include split variables or factor variables or both. List split variables, if any, on the <code>SPLIT</code> subcommand and factor variables, if any, on the <code>FACTORS</code> subcommand. Split and factor variables must be numeric. Split and factor variables must also be listed on <code>VARIABLES</code>, with one exception: if <code>VARIABLES</code> does not include <code>ROWTYPE_</code>, then <code>SPLIT</code> may name a single variable that is not in <code>VARIABLES</code> (see <a href="./MATRIX-DATA-without-ROWTYPE_005f#MATRIX-DATA-Example-8">MATRIX DATA Example 8</a>).</p><p>The <code>FORMAT</code> subcommand accepts settings to describe the format of the input data:</p><p><code>LIST</code> (default) <code>FREE</code></p><p>LIST requires each row to begin at the start of a new input line. FREE allows rows to begin in the middle of a line. Either setting allows a single row to continue across multiple input lines.</p><p><code>LOWER</code> (default) <code>UPPER</code><code>FULL</code></p><p>With LOWER, only the lower triangle is read from the input data and the upper triangle is mirrored across the main diagonal. UPPER behaves similarly for the upper triangle. FULL reads the entire matrix.</p><p><code>DIAGONAL</code> (default) <code>NODIAGONAL</code></p><p>With DIAGONAL, the main diagonal is read from the input data. With NODIAGONAL, which is incompatible with FULL, the main diagonal is not read from the input data but instead set to 1 for correlation matrices and system-missing for others.</p><p>The <code>N</code> subcommand is a way to specify the size of the population. It is equivalent to specifying an <code>N</code> vector with the specified value for each split file.</p><p><code>MATRIX DATA</code> supports two different ways to indicate the kinds of matrices and vectors present in the data, depending on whether a variable with the special name <code>ROWTYPE_</code> is present in <code>VARIABLES</code>. The following subsections explain <code>MATRIX DATA</code> syntax and behavior in each case.</p><table tabindex="0"><thead><tr><th>• With ROWTYPE_</th><th></th><th></th></tr></thead><tbody><tr><td>• Without ROWTYPE_</td><td></td><td></td></tr></tbody></table>`,18)])])}const A=t(n,[["render",d]]);export{m as __pageData,A as default};
