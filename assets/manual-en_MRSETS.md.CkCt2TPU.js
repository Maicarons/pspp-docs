import{_ as s,o as a,c as t,a2 as o}from"./chunks/framework.Bg-Zxr5O.js";const p=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/MRSETS.md","filePath":"manual-en/MRSETS.md","lastUpdated":1785684581000}'),i={name:"manual-en/MRSETS.md"};function l(n,e,r,c,d,u){return a(),t("div",null,[...e[0]||(e[0]=[o(`<h3 id="_11-20-mrsets" tabindex="-1">11.20 MRSETS <a class="header-anchor" href="#_11-20-mrsets" aria-label="Permalink to &quot;11.20 MRSETS&quot;">​</a></h3><p><code>MRSETS</code> creates, modifies, deletes, and displays multiple response sets. A multiple response set is a set of variables that represent multiple responses to a survey question.</p><p>Multiple responses are represented in one of the two following ways:</p><ul><li><p>A <em>multiple dichotomy set</em> is analogous to a survey question with a set of checkboxes. Each variable in the set is treated in a Boolean fashion: one value (the &quot;counted value&quot;) means that the box was checked, and any other value means that it was not.</p></li><li><p>A <em>multiple category set</em> represents a survey question where the respondent is instructed to list up to n choices. Each variable represents one of the responses.</p><p>MRSETS /MDGROUP NAME=name VARIABLES=var_list VALUE=value [CATEGORYLABELS={VARLABELS,COUNTEDVALUES}] [{LABEL=’label’,LABELSOURCE=VARLABEL}]</p><pre><code>/MCGROUP NAME=name VARIABLES=var_list [LABEL=’label’]

/DELETE NAME={[names],ALL}

/DISPLAY NAME={[names],ALL}
</code></pre></li></ul><p>Any number of subcommands may be specified in any order.</p><p>The <code>MDGROUP</code> subcommand creates a new multiple dichotomy set or replaces an existing multiple response set. The <code>NAME</code>, <code>VARIABLES</code>, and <code>VALUE</code> specifications are required. The others are optional:</p><ul><li>NAME specifies the name used in syntax for the new multiple dichotomy set. The name must begin with ‘$’; it must otherwise follow the rules for identifiers (see <a href="./Tokens">Tokens</a>).</li><li><code>VARIABLES</code> specifies the variables that belong to the set. At least two variables must be specified. The variables must be all string or all numeric.</li><li><code>VALUE</code> specifies the counted value. If the variables are numeric, the value must be an integer. If the variables are strings, then the value must be a string that is no longer than the shortest of the variables in the set (ignoring trailing spaces).</li><li><code>CATEGORYLABELS</code> optionally specifies the source of the labels for each category in the set: <ul><li>- <code>VARLABELS</code>, the default, uses variable labels or, for variables without variable labels, variable names. PSPP warns if two variables have the same variable label, since these categories cannot be distinguished in output.</li><li>- <code>COUNTEDVALUES</code> instead uses each variable’s value label for the counted value. PSPP warns if two variables have the same value label for the counted value or if one of the variables lacks a value label, since such categories cannot be distinguished in output.</li></ul></li><li><code>LABEL</code> optionally specifies a label for the multiple response set. If neither <code>LABEL</code> nor <code>LABELSOURCE=VARLABEL</code> is specified, the set is unlabeled.</li><li><code>LABELSOURCE=VARLABEL</code> draws the multiple response set’s label from the first variable label among the variables in the set; if none of the variables has a label, the name of the first variable is used. <code>LABELSOURCE=VARLABEL</code> must be used with <code>CATEGORYLABELS=COUNTEDVALUES</code>. It is mutually exclusive with <code>LABEL</code>.</li></ul><p>The <code>MCGROUP</code> subcommand creates a new multiple category set or replaces an existing multiple response set. The <code>NAME</code> and <code>VARIABLES</code> specifications are required, and <code>LABEL</code> is optional. Their meanings are as described above in <code>MDGROUP</code>. PSPP warns if two variables in the set have different value labels for a single value, since each of the variables in the set should have the same possible categories.</p><p>The <code>DELETE</code> subcommand deletes multiple response groups. A list of groups may be named within a set of required square brackets, or ALL may be used to delete all groups.</p><p>The <code>DISPLAY</code> subcommand displays information about defined multiple response sets. Its syntax is the same as the <code>DELETE</code> subcommand.</p><p>Multiple response sets are saved to and read from system files by, <em>e.g.</em> , the <code>SAVE</code> and <code>GET</code> command. Otherwise, multiple response sets are currently used only by third party software.</p>`,11)])])}const m=s(i,[["render",l]]);export{p as __pageData,m as default};
