import{_ as t,o as a,c as o,a2 as s}from"./chunks/framework.Bg-Zxr5O.js";const f=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"manual-en/Syntax.md","filePath":"manual-en/Syntax.md","lastUpdated":1785684581000}'),i={name:"manual-en/Syntax.md"};function d(n,e,c,r,l,h){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h4 id="_15-17-1-syntax" tabindex="-1">15.17.1 Syntax <a class="header-anchor" href="#_15-17-1-syntax" aria-label="Permalink to &quot;15.17.1 Syntax&quot;">​</a></h4><pre><code>REGRESSION
        /VARIABLES=var_list
        /DEPENDENT=var_list
        /STATISTICS={ALL, DEFAULTS, R, COEFF, ANOVA, BCOV, CI[conf, TOL]}
        { /ORIGIN | /NOORIGIN }
        /SAVE={PRED, RESID}
</code></pre><p>The <code>REGRESSION</code> procedure reads the active dataset and outputs statistics relevant to the linear model specified by the user.</p><p>The <code>VARIABLES</code> subcommand, which is required, specifies the list of variables to be analyzed. Keyword <code>VARIABLES</code> is required. The <code>DEPENDENT</code> subcommand specifies the dependent variable of the linear model. The <code>DEPENDENT</code> subcommand is required. All variables listed in the <code>VARIABLES</code> subcommand, but not listed in the <code>DEPENDENT</code> subcommand, are treated as explanatory variables in the linear model.</p><p>All other subcommands are optional:</p><p>The <code>STATISTICS</code> subcommand specifies which statistics are to be displayed. The following keywords are accepted:</p><p><code>ALL</code></p><p>All of the statistics below.</p><p><code>R</code></p><p>The ratio of the sums of squares due to the model to the total sums of squares for the dependent variable.</p><p><code>COEFF</code></p><p>A table containing the estimated model coefficients and their standard errors.</p><p><code>CI (conf)</code></p><p>This item is only relevant if COEFF has also been selected. It specifies that the confidence interval for the coefficients should be printed. The optional value conf, which must be in parentheses, is the desired confidence level expressed as a percentage.</p><p><code>ANOVA</code></p><p>Analysis of variance table for the model.</p><p><code>BCOV</code></p><p>The covariance matrix for the estimated model coefficients.</p><p><code>TOL</code></p><p>The variance inflation factor and its reciprocal. This has no effect unless COEFF is also given.</p><p><code>DEFAULT</code></p><p>The same as if R, COEFF, and ANOVA had been selected. This is what you get if the /STATISTICS command is not specified, or if it is specified without any parameters.</p><p>The <code>ORIGIN</code> and <code>NOORIGIN</code> subcommands are mutually exclusive. <code>ORIGIN</code> indicates that the regression should be performed through the origin. You should use this option if, and only if you have reason to believe that the regression does indeed pass through the origin — that is to say, the value <em>b_0</em> above, is zero. The default is <code>NOORIGIN</code>.</p><p>The <code>SAVE</code> subcommand causes PSPP to save the residuals or predicted values from the fitted model to the active dataset. PSPP will store the residuals in a variable called ‘RES1’ if no such variable exists, ‘RES2’ if ‘RES1’ already exists, ‘RES3’ if ‘RES1’ and ‘RES2’ already exist, etc. It will choose the name of the variable for the predicted values similarly, but with ‘PRED’ as a prefix. When <code>SAVE</code> is used, PSPP ignores <code>TEMPORARY</code>, treating temporary transformations as permanent.</p>`,24)])])}const m=t(i,[["render",d]]);export{f as __pageData,m as default};
