### 11.12 ADD VALUE LABELS

`ADD VALUE LABELS` has the same syntax and purpose as `VALUE LABELS` (see [VALUE LABELS](VALUE-LABELS.md)), but it does not clear value labels from the variables before adding the ones specified. 
    
    
    ADD VALUE LABELS
            /var_list value ’label’ [value ’label’]…
