### 17.10 ERASE
    
    
    ERASE FILE file_name.
    

`ERASE FILE` deletes a file from the local file system. file_name must be quoted. This command cannot be used if the SAFER (see [SET](SET.md)) setting is active.
