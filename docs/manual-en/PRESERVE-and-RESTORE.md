### 17.19 PRESERVE and RESTORE
    
    
    PRESERVE.
    …
    RESTORE.
    

`PRESERVE` saves all of the settings that `SET` (see [SET](SET.md)) can adjust. A later `RESTORE` command restores those settings. 

`PRESERVE` can be nested up to five levels deep.
