### 17.8 DROP DOCUMENTS
    
    
    DROP DOCUMENTS.
    

`DROP DOCUMENTS` removes all documents from the active dataset. New documents can be added with `DOCUMENT` (see [DOCUMENT](DOCUMENT.md)). 

`DROP DOCUMENTS` changes only the active dataset. It does not modify any system files stored on disk.
