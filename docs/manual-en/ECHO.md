### 17.9 ECHO
    
    
    ECHO ’arbitrary text’ .
    

Use `ECHO` to write arbitrary text to the output stream. The text should be enclosed in quotation marks following the normal rules for string tokens (see [Tokens](Tokens.md)).
