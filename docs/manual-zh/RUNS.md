#### 15.12.11 游程检验


         [ /RUNS ({MEAN, MEDIAN, MODE, value})  = var_list ]


`/RUNS` 子命令检验一个数据序列是否随机排序。

其原理是检查某个变量取值跨越给定阈值的次数。所需的阈值必须在括号内指定。它可以是一个数字，也可以是 `MEAN`、`MEDIAN` 或 `MODE` 之一。在阈值说明之后是待检验取值的变量列表。

该子命令显示游程数，以及基于数据长度的渐近显著性。
