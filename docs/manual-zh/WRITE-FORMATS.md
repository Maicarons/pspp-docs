### 11.9 WRITE FORMATS
    
    
    WRITE FORMATS var_list (fmt_spec) [var_list (fmt_spec)]….
    

`WRITE FORMATS` 将指定变量的写格式设置为指定的格式说明。其语法与 `FORMATS`（见 [FORMATS](FORMATS.md)）相同，但 `WRITE FORMATS` 只设置写格式，而不设置打印格式。
