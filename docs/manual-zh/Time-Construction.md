#### 7.7.8.2 生成时间的函数

这些函数接受数值参数，并返回表示时间的数值。

函数：__**TIME.DAYS** _( ndays)_
    

返回对应 ndays 天的时长。

函数：__**TIME.HMS** _( nhours, nmins, nsecs)_
    

返回对应 nhours 小时、nmins 分钟和 nsecs 秒的时长。参数不能带有混合符号：如果其中任意参数为正，则其余均不能为负，反之亦然。
