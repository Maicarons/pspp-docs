### 11.12 ADD VALUE LABELS

`ADD VALUE LABELS` 的语法和用途与 `VALUE LABELS` 相同（参见 [VALUE LABELS](VALUE-LABELS.md)），但在添加指定的值标签之前，不会清除变量原有的值标签。
    
    
    ADD VALUE LABELS
            /var_list value ’label’ [value ’label’]…
