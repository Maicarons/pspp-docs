#### 15.13.3 配对样本模式

`PAIRS` 子命令启用配对样本模式。当对同一样本进行了重复测量时，使用此模式。如果省略 `WITH` 关键字，则会生成 `PAIRS` 子命令中给出的所有变量组合对应的表格。如果给出了 `WITH` 关键字，且同时给出了 `(PAIRED)` 关键字，则 `WITH` 之前的变量数量必须与之后的变量数量相同。在这种情况下，会针对每对相应的变量生成表格。如果给出了 `WITH` 关键字但省略了 `(PAIRED)` 关键字，则会生成 `WITH` 之前的每个变量与 `WITH` 之后的每个变量组合对应的表格。
