#### 15.7.1 基础

唯一必需的子命令是 `TABLE`，它指定沿每个轴包含的变量：

    
    /TABLE _rows_ [BY _columns_ [BY _layers_]]
    

在 `TABLE` 中，行、列和层各自要么为空，要么是一个指定一个或多个变量的轴表达式。至少有一个必须指定轴表达式。

| • 分类变量 |  |  |
| --- | --- | --- |
| • 标量变量 |  |  |
| • 覆盖测量级别 |  |  |
