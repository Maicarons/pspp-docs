### 11.8 PRINT FORMATS

    PRINT FORMATS var_list (fmt_spec) [var_list (fmt_spec)]….


`PRINT FORMATS` 将指定变量的打印格式设置为指定的格式说明。

其语法与 `FORMATS`（参见 [FORMATS](FORMATS.md)）相同，但 `PRINT FORMATS` 只设置打印格式，而不设置写入格式。
