## 4 调用 `psppire`

### 4.1 图形用户界面

PSPP 的 PSPPIRE 图形用户界面可以执行命令行界面的全部功能。此外，它能即时显示数据、变量和统计输出。

图形用户界面可通过在命令提示符下输入 `psppire` 来启动。此外，许多系统提供交互式菜单或按钮，通过一系列鼠标点击即可启动 `psppire`。

一旦理解了 PSPP 系统的基本原理，图形用户界面在设计上基本直观，因此本手册仅作非常简要的介绍。
